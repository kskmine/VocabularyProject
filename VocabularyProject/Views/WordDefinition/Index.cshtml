@model IEnumerable<VocabularyProject.Models.WordDefinitionViewModel>

@{ ViewData["Title"] = "Index"; }

<h1>WORD DEFINITION PAGE</h1>
<div class="row">
    <div class="col-md-2">
        <p>
            <a asp-action="Edit">Yeni Kelime Ekle</a>
        </p>
    </div>
    <div class="col-md-6"><input id="searchKeyword" onkeypress="refreshByKey(event)" type="text" class="form-control" /></div>
    <div class="col-md-1">
        <select class="form-control" id="MeaninLangId" name="MeaninLangId">
            <option title="Turkish" value="1">tr</option>
            <option title="English" value="2">eng</option>
        </select>
        <button type="button" onclick="refresh()" class="btn btn">
            <i class="bi bi-search"></i>
        </button>

    </div>
</div>
@*<div id="loading">
        <img height="130" src="~/images/Loading_icon.gif" />
    </div>*@
<div id="listTable">
    @*<partial name="ListPartial" model="Model"  />*@
</div>





<style>
   
    #wordsTable {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

        #wordsTable td, #customers th {
            border: 1px solid #ddd;
            padding: 8px;
        }

        /* #wordsTable tr:nth-child(even) {
                background - color: #f2f2f2;
                    }
            */
        #wordsTable .trMaster:hover {
            background-color: #ddd;
        }

        #wordsTable th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #3a2860;
            color: white;
        }
    /* table {
                font - family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
            }

            td, th {
                border: 1px solid #eff6fe;
            text-align: left;
            padding: 8px;
            }

            tr:nth-child(even) {
                background - color: #eff6fe;
            }*/
</style>

@section Scripts{
    <script>
        function showHideDetail(elemId) {
            $("#" + elemId).toggle();
        }
        function refresh() {
            $('#loading').show();
            $('#listTable').html(' <img height="130" src="/images/Loading_icon.gif" />');
            var searchKeyword = $('#searchKeyword').val();

            $('#listTable').load('/WordDefinition/ListPartial', { 'searchKeyword': searchKeyword });
            // window.location.href = '/WordDefinition/Index?searchKeyword=' + searchKeyword;
            //$('#listTable').load('/WordDefinition/ListPartial?searchKeyword=' + searchKeyword
            //    , {}, function () { $('#loading').hide(); }
            //);

        }

        function refreshByKey(e) {
            if (e.keyCode === 13) {
                e.preventDefault();
                refresh();
            }
        }

        $(document).ready(function () {
            refresh();
        });

    </script>
}
